// var result = document.getElementById('result');
var num = 6;
for(var i=1;i<=num;i++) {
    if(i%2 == 0) {
       console.log(`Even Number : ${i}`);
    //    result.textContent = `Even Number : ${i}`;
    }
    else {
        console.log(`Odd Number : ${i}`);
        // result.textContent = `Odd Number : ${i}`;
    }
}